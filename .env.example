# Puerto del servidor
PORT=10000

# Entorno de ejecución
NODE_ENV=production

# Directorio de persistencia de datos (Opcional, por defecto ./data)
# DATA_DIR=/var/data

# URL de Base de Datos PostgreSQL (Opcional, para Render Postgres)
# DATABASE_URL=postgresql://user:password@hostname:5432/dbname
